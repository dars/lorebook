## 1. Domain

- [x] 1.1 新增內建語言清單常數（16 種；標準 8 ＋ 稀有 8，見 design D1），註明刻意未收 2024 新增的三種與其理由
- [x] 1.1b 建角送出時預設帶入「通用語」；內建範例角色的語言改為中文（與清單同語系，避免編輯時同義重複）
- [x] 1.2 `CurrentCharacterNotifier` 新增 `addLanguage`／`removeLanguage`：加入時去空白、去重；不排序

## 2. 編輯介面

- [x] 2.1 熟練區段標題列加 `SectionEditIcon`（沿用「其人其事」「性格」慣例）
- [x] 2.2 新增編輯 sheet：內建清單以 chip 選取、另有自由填寫欄位；**版面預留工具熟練的位置**（分段呈現），供 `tool-proficiency-editing` 接入
- [x] 2.3 已加入者於清單中呈現為已選狀態，可點擊移除
- [x] 2.4 空狀態文案改為指向編輯入口（現為「尚未有語言或工具熟練」）
- [x] 2.5 觸控目標 ≥ 48dp；單筆長度上限 20 字

## 3. 測試

- [x] 3.1 notifier 測試：新增、移除、去重（含前後空白）、順序維持
- [x] 3.2 widget 測試：自清單選取與自由填寫皆可加入；移除生效
- [x] 3.3 widget 測試：熟練為空時仍可開啟編輯並加入第一項
- [x] 3.3b 預設語言測試：建角後含通用語；範例角色的語言皆屬內建清單
- [x] 3.4 內建清單測試：16 種皆在，且不含未查證的三種（通用手語／德魯伊語／盜賊黑話）

## 4. 收尾

- [x] 4.1 `flutter analyze` 與 `dart format .` 全數通過
- [x] 4.2 `flutter test` 全綠
- [x] 4.3 dev server 確認：新增／移除語言、重新載入後仍在（寫入雲端）——2026-08-04 使用者驗證通過
