## 1. 英雄卡配色

- [x] 1.1 `_Hero` 漸層由紫藍改暖金/暗黑（`#2E2418 → #1E160C → #14110C`）；保留浮水印、底部加深、名稱與覆蓋資訊
- [x] 1.2 高度微縮（340→~320），可加極淡金色頂線（選用）

## 2. 次級分頁底線式

- [x] 2.1 `CharacterTabBar` 由膠囊外框改為無框底線式（文字 + 底線指示，選取金色），維持水平捲動

## 3. 總覽密度與一致性

- [x] 3.1 `_InfoGrid` 列距 lg→md；`InfoField` 值 16→15
- [x] 3.2 總覽「基本資訊 / 戰鬥數值」改用與行動頁相同的 `CollapsibleSection`（強標頭 + 可收合），不用 `SectionTitle`
- [x] 3.4 屬性/法術/物品/傳記分頁的各區段一律改用 `CollapsibleSection`（取代 `SectionTitle`）
- [x] 3.3 `_StatCards` 確認金色/暗黑主題一致

## 4. 驗證

- [x] 4.1 `flutter analyze` 無錯誤
- [x] 4.2 實機驗證：英雄卡暖金配色、底線分頁切換正常、資訊密度與區段標頭
- [x] 4.3 驗證手機與平板版型呈現正常
